{
  "name": "file-mover",
  "version": "1.0.0",
  "description": "small project that will move files from one folder to another",
  "main": "dist/src/index.js",
  "type": "module",
  "scripts": {
    "pre-commit": "npm run typecheck && npm run find-deadcode && lint-staged",
    "build": "npm run clean && tsc",
    "clean": "rimraf dist/",
    "dev": "NODE_ENV=development npm run build && node --trace-warnings dist/src/index.js",
    "format": "prettier --write .",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --skipLibCheck"
  },
  "author": "Tudor Roman",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "js-yaml": "^4.1.0",
    "pino": "^8.14.1",
    "pino-pretty": "^10.0.1"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.5",
    "@types/node": "^20.4.1",
    "prettier": "^3.0.0"
  }
}
